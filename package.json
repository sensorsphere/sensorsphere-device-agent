{
  "name": "sensorsphere-device-agent",
  "version": "1.0.17",
  "private": true,
  "description": "SensorSphere remote device discovery and control agent",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "npm run build && node --test dist/tests"
  },
  "engines": {
    "node": ">=22.20"
  },
  "dependencies": {
    "multicast-dns": "^7.2.5",
    "esphome-client": "^2.0.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
